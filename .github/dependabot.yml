version: 2
updates:
  # Keeps the SHA-pinned GitHub Actions up to date (pull requests instead of silent updates)
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
